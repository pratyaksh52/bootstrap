# Use lesspipe
eval $(lesspipe)

# Add GO Path
export PATH=$PATH:/usr/local/go/bin

# Add exercism binary to path
export PATH=$PATH:~/bin

# Add bob to path
export PATH="$PATH:/home/chopr/.local/share/bob/nvim-bin"
